#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Succes)
 */
 
int main(void)
{          
         int n,lastd;
	    
	 srand(time(0));
	 n =rand() -RAND_MAX / 2;

	 lastd = n % 10;
	    
	 if (lastd  > 5)
	          {
	            printf("%d and is greater than 5\n", last_digit);
	          }
         else if (lastd == 0)
	          {
	           printf("%d and is 0\n", last_digit);
		  } 
	 else
	       {
                printf("%d and is less than 6 and not 0\n",
	       }	
	  return (0);
	                                                                                                                                             }	    
	    	    	    

